<UserControl x:Class="CoopCheck.WPF.Content.NoAccessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <ScrollViewer>
            <StackPanel MinWidth="200">
                <TextBlock Text="Not Authorized" Margin="8" Style="{StaticResource Heading2}" Foreground="Red" />
                <TextBlock Margin="16" Text="{Binding UserName}" Style="{StaticResource Heading2}" />
                <TextBlock Text="You are not authorized to use this application"
                           Foreground="Red" Style="{StaticResource Heading2}" Margin="8" />
                <TextBlock Text="Contact the help desk for assistance"
                           Foreground="Red" Style="{StaticResource Heading2}" Margin="8" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>