<UserControl xmlns:vouchers="clr-namespace:muiCoopCheck.Pages.Vouchers"  x:Class="muiCoopCheck.Pages.Vouchers.AddVouchersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
            <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <Grid >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <vouchers:ImportView x:Name="iv" Grid.Row="0"  />
                <vouchers:BatchEditView x:Name="bev" Grid.Row="0" />
                <vouchers:ValidateView x:Name="vv" Grid.Row="0"  />
                <Button x:Name="btnDOIT" IsEnabled="{Binding CanProceed}" Click="Button_Click" Grid.Row="1">Done</Button>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
