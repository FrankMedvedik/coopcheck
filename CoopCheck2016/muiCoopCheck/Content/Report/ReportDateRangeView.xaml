<UserControl x:Class="CoopCheck.WPF.Content.Report.ReportDateRangeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ignore="http://www.ignore.com"
             mc:Ignorable="d ignore">
        <Grid>
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Horizontal" >
                    <TextBlock Text="Start"  Width="35" Margin="6" VerticalAlignment="Center" HorizontalAlignment="Right" />
                <DatePicker  x:Name="pckStart" ToolTipService.ToolTip="start date for the report" SelectedDate="{Binding StartRptDate, Mode=TwoWay,StringFormat=d}" Margin="0,10,0,9"  />
                </StackPanel>
                <StackPanel Orientation="Horizontal" >
                    <TextBlock Text="End" Width="35" Margin="6" VerticalAlignment="Center" HorizontalAlignment="Right" />
                <DatePicker x:Name="pckEnd" ToolTipService.ToolTip="end date for the report" SelectedDate="{Binding EndRptDate, Mode=TwoWay,StringFormat=d}" Margin="0,10,0,9" />
                </StackPanel>
            </StackPanel>
        </Grid>
</UserControl>
    
