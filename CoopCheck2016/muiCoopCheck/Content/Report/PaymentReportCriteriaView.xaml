<UserControl x:Class="CoopCheck.WPF.Content.Report.PaymentReportCriteriaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.ignore.com"
        xmlns:bankAccount="clr-namespace:CoopCheck.WPF.Content.BankAccount"
        mc:Ignorable="d ignore">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
            <WrapPanel Orientation="Vertical" >
                <StackPanel Orientation="Horizontal" x:Name="spAccount">
                    <bankAccount:AccountSelectView x:Name="asv"/>
                    <CheckBox Content="Printed" IsChecked="{Binding PaymentReportCriteria.IsPrinted}" Margin="6"/>
                    <CheckBox Content="Cleared" IsChecked="{Binding PaymentReportCriteria.IsCleared}" Margin="6"/>
                </StackPanel>
            <StackPanel Orientation="Horizontal" x:Name="spReportDates">
                <TextBlock Text="Start Date" Width="100"   Margin="6"/>
                <DatePicker SelectedDate="{Binding PaymentReportCriteria.StartDate,StringFormat=d, Mode=TwoWay}" Margin="6"  />
                <TextBlock Text="End Date" Width="100"   HorizontalAlignment="Right" Margin="6"/>
                <DatePicker SelectedDate="{Binding PaymentReportCriteria.EndDate, StringFormat=d, Mode=TwoWay}" Margin="6"  />
            </StackPanel>
            <StackPanel Orientation="Horizontal" x:Name="spCheckNumber">
                    <TextBlock Text="Check Number" Width="100"   Margin="6"/>
                    <TextBox Width="300" Text="{Binding PaymentReportCriteria.CheckNumber, Mode=TwoWay}" Margin="6" ToolTip="the check or (swift) payment code you are looking for"/>
                </StackPanel>
            <StackPanel Orientation="Horizontal"  x:Name="spJob">
                    <TextBlock Text="Job" Width="100"  Margin="6" ToolTip="enter all or part of the job number you are looking for" />
                <TextBox Width="110" Text="{Binding PaymentReportCriteria.JobNumber, Mode=TwoWay}" Margin="6" />
                </StackPanel>
            <StackPanel Orientation="Vertical" x:Name="spPayee">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Email" Width="100"  Margin="6" ToolTip="enter all or part of the email address you are looking for" />
                <TextBox Width="300" Text="{Binding PaymentReportCriteria.Email, Mode=TwoWay}" Margin="6" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Phone" Width="100"  Margin="6"  />
                    <TextBox Width="300" Text="{Binding PaymentReportCriteria.PhoneNumber, Mode=TwoWay}" Margin="6" ToolTip="enter all or part of the phone number you are looking for"/>
            </StackPanel>
                <StackPanel Orientation="Horizontal" >
                    <TextBlock Text="Last Name" Margin="6" Width="100"  ToolTip="enter all or part of the last name you are looking for"/>
                <TextBox Width="300" Text="{Binding PaymentReportCriteria.LastName, Mode=TwoWay}" Margin="6" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" >
                    <TextBlock Text="First Name" Margin="6" Width="100" ToolTip="enter all or part of the first name you are looking for"/>
                <TextBox Width="300" Text="{Binding PaymentReportCriteria.FirstName, Mode=TwoWay}" Margin="6" />
            </StackPanel>
            </StackPanel>
        </WrapPanel>
    </Grid>
</UserControl>
