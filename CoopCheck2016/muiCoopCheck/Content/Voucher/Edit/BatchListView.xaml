<UserControl xmlns:Voucher="clr-namespace:CoopCheck.WPF.Content.Voucher"  x:Class="CoopCheck.WPF.Content.Voucher.Edit.BatchListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
        <Grid Style="{StaticResource ContentRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
            <!--<mui:ModernProgressRing IsActive="{Binding IsBusy}" Grid.Row="0" Width="80" Height="80" Style="{StaticResource ChasingDotsProgressRingStyle}" />-->
        <DataGrid AutoGenerateColumns="False" Grid.Column="0" Visibility="{Binding IsBusy, Converter={StaticResource NotBoolToVisConverter}}" 
                IsReadOnly="True" HorizontalAlignment="Left" VerticalAlignment="Top"  Margin="12" MaxHeight="250"
                  ClipboardCopyMode="IncludeHeader" EnableColumnVirtualization="True" EnableRowVirtualization="True"
                ItemsSource="{Binding BatchList}" HorizontalScrollBarVisibility="Auto" Height="Auto" 
                VerticalScrollBarVisibility="Auto" SelectedItem="{Binding SelectedBatch, Mode=TwoWay}">
                <DataGrid.Columns>
                 <DataGridTextColumn Binding="{Binding batch_num }" Header="Batch Number"/>
                <DataGridTextColumn Binding="{Binding job_num }" Header="Job" />
                <DataGridTextColumn Binding="{Binding batch_date, StringFormat=d}" Header="Create Date" />
                <DataGridTextColumn Binding="{Binding pay_date, StringFormat=d}" Header="Pay Date" />
                <DataGridTextColumn Binding="{Binding voucher_cnt}" Header="Count" />
                <DataGridTextColumn Binding="{Binding total_amount,StringFormat=c}" Header="Dollars" />
            </DataGrid.Columns>
            </DataGrid>
        <!--<Button Grid.Row="1" Content="Refresh" HorizontalAlignment="Left" Click="Button_Click" Width="100" Margin="12" VerticalAlignment="Center"/>-->
    </Grid>
</UserControl>
