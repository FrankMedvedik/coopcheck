<UserControl x:Class="CoopCheck.WPF.Content.BankAccount.AccountSelectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
        <StackPanel Orientation="Horizontal" >
            <TextBlock Text="Account" Width="100"   Margin="6"/>
            <ComboBox x:Name="ComboAccount" Width="150" ItemsSource="{Binding Accounts}" 
                          DisplayMemberPath="account_name" 
            SelectedValue="{Binding SelectedAccount}">
            </ComboBox>
        </StackPanel>
</UserControl>
