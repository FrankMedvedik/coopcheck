<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:mui="http://firstfloorsoftware.com/ModernUI">
    <Grid.ColumnDefinitions >
        <ColumnDefinition/>
        <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <DataGrid AutoGenerateColumns="True" IsReadOnly="True" HorizontalAlignment="Left"   VerticalAlignment="Top"  MaxHeight="800" Grid.Column="0"
                MaxWidth="1000" MinHeight="200"  ItemsSource="{Binding BankFileViewModel.BankClearTransactions}" HorizontalScrollBarVisibility="Auto"   
                      Height="Auto" VerticalScrollBarVisibility="Auto"  SelectionMode="Single" >
   </DataGrid>
</Grid>
