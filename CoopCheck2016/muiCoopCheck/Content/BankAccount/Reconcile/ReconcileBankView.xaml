<UserControl x:Class="CoopCheck.WPF.Content.BankAccount.Reconcile.ReconcileBankView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" >
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <mui:ModernProgressRing IsActive="{Binding IsBusy}" Width="80" Height="80" Style="{StaticResource CircleProgressRingStyle}" Grid.Row="0"/>
        <!--<xctk:BusyIndicator IsBusy="{Binding IsBusy}">-->
        <Grid Visibility="{Binding CanImport, Converter={StaticResource BoolToVisConverter}}" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Vertical"  Grid.Row="0">
                <StackPanel Orientation="Horizontal" >
                    <Button Content="Select File" Name="btnOpen" Width="150"  Height="30" Margin="6" HorizontalAlignment="Left"  Click="btnOpen_Click" />
                    <TextBox Height="30" Name="txtFilePath" Width="500" Text="{Binding ExcelFilePath, Mode=TwoWay, ValidatesOnDataErrors=True}"  Margin="6" HorizontalAlignment="Left"/>
                </StackPanel>
            </StackPanel>
            <Grid Grid.Row="1" Visibility="{Binding CanReconcile, Converter={StaticResource BoolToVisConverter}}" Name="GridVouchers">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Vertical" Grid.Row="0"  >
                    <TextBlock Style="{StaticResource Emphasis}" HorizontalAlignment="Left" Margin="6">File Details</TextBlock>
                    <StackPanel Orientation="Horizontal"  >
                        <TextBlock Style="{StaticResource Emphasis}" HorizontalAlignment="Left" Margin="6">Credits</TextBlock>
                        <TextBlock Style="{StaticResource Emphasis}" Margin="6" Width="100" HorizontalAlignment="Left" Text="{Binding VoucherCnt}"/>
                        <TextBlock Style="{StaticResource Emphasis}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="6">Debits</TextBlock>
                        <TextBlock  Style="{StaticResource Emphasis}" VerticalAlignment="Center" Margin="6" Width="100" HorizontalAlignment="Left" Text="{Binding VoucherTotalDollars, StringFormat=c }"/>
                    </StackPanel>
                </StackPanel>
                <Button Grid.Row="1" HorizontalAlignment="Left" Margin="172,6,6,6" Content="Next"  Width="100" IsDefault="True" Click="Next_Click"/>
            </Grid>
        </Grid>
        <StackPanel Orientation="Vertical" Grid.Row="2">
            <TextBlock  Style="{StaticResource Emphasis}" VerticalAlignment="Center" Margin="6" HorizontalAlignment="Left" Text="RECONCILIATION REPORT PLACEHOLD"/>
            <DataGrid ItemsSource="{Binding BankClearTransactions, Mode=TwoWay}" AutoGenerateColumns="True" MaxHeight="800" />
        </StackPanel>
        <Button Grid.Row="3" HorizontalAlignment="Left" Margin="172,6,6,6" Content="Close"  Width="100" x:Name="btnClose" Visibility="Collapsed" VerticalAlignment="Center" Click="Close_Click"/>
    </Grid>
</UserControl>
